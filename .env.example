# Sha Silvers — environment
#
# Copy to `.env` and fill in. With no store variables set, Hydrogen falls back
# to Shopify's public mock.shop catalogue, so `npm run dev` works immediately —
# useful for building UI, but the products are Shopify's demo apparel, not
# silverware.

# Signs the session cookie. Any long random string; change it per environment.
SESSION_SECRET="change-me"

# --- Point at a real store -------------------------------------------------
# Run `npx shopify hydrogen link` to fill these in automatically, or set them
# by hand from Settings → Apps → Headless in the Shopify admin.
#
# PUBLIC_STORE_DOMAIN="shasilvers.myshopify.com"
# PUBLIC_STOREFRONT_API_TOKEN=""
# PRIVATE_STOREFRONT_API_TOKEN=""
# PUBLIC_STOREFRONT_ID=""
# PUBLIC_CHECKOUT_DOMAIN="checkout.shasilvers.com"

# --- Customer accounts (order tracking, saved addresses) -------------------
# PUBLIC_CUSTOMER_ACCOUNT_API_CLIENT_ID=""
# PUBLIC_CUSTOMER_ACCOUNT_API_URL=""
